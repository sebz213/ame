{
  "$description": "Component layer, namespaced so it cannot collide with base or semantic keys. Colour, duration, and easing are always references to semantic. Per-component measures are declared here as literals, because a measure used by exactly one element has no shared scale to sit on; that permission is stated in contract.md > Invariant L3 and checked.",
  "component": {
    "glass": {
      "blur": { "$type": "dimension", "$value": "{surface.glass-blur}" },
      "saturate": { "$type": "number", "$value": "{surface.glass-saturate}" },
      "edge": { "$type": "shadow", "$value": "{elevation.glass-edge}" },
      "edge-light": { "$type": "shadow", "$value": "{elevation.glass-edge-light}" },
      "edge-on-dark": { "$type": "shadow", "$value": "{elevation.glass-edge-on-dark}" },
      "edge-light-on-dark": { "$type": "shadow", "$value": "{elevation.glass-edge-light-on-dark}" },
      "drop": { "$type": "shadow", "$value": "{elevation.glass-drop}" },
      "drop-on-dark": { "$type": "shadow", "$value": "{elevation.glass-drop-on-dark}" },
      "fill-opaque": { "$type": "color", "$value": "{surface.glass-fill-opaque-on-light}" },
      "fill-opaque-on-dark": { "$type": "color", "$value": "{surface.glass-fill-opaque-on-dark}" }
    },

    "nav": {
      "item-gap": {
        "$type": "dimension",
        "$value": { "value": 0.25, "unit": "rem" },
        "$description": "4px (on the spacing grid), the gap between nav pills. Single home: the rendered flex gap (gap-[0.25rem]) must equal it (invariant P2). The pills themselves size to their labels, so there is no longer an item-width token."
      },
      "pad": {
        "$type": "dimension",
        "$value": { "value": 0.25, "unit": "rem" },
        "$description": "Segmented-pill inner padding, the ring around the items. One home for the nav, the 3D/2D toggle, and the accessibility Segmented; the toggle and Segmented read var(--component-nav-pad)."
      },
      "item-height": {
        "$type": "dimension",
        "$value": { "value": 1.75, "unit": "rem" },
        "$description": "Segmented-pill item height. One home for the nav, the 3D/2D toggle, and the accessibility Segmented; they read var(--component-nav-item-height)."
      },
      "logo-gap": {
        "$type": "dimension",
        "$value": { "value": 12, "unit": "px" },
        "$description": "Logo lockup (comb + Sébastien wordmark) to glass nav, 12px (on the 4px grid). The wordmark's right spacing (marginRight 3 + this = 15) is set equal to its left (marginLeft 15) for symmetry. Single home: site-header.tsx LOGO_GAP must equal it (invariant P1)."
      },
      "offset-fallback": {
        "$type": "dimension",
        "$value": { "value": 60, "unit": "px" },
        "$description": "Nav offset used before the logo has measured. Single home: site-header.tsx NAV_OFFSET_FALLBACK (invariant P1)."
      },
      "pill-bg": { "$type": "color", "$value": "{nav.bg}" },
      "pill-bg-on-dark": { "$type": "color", "$value": "{nav.bg-on-dark}" },
      "pill-shadow": { "$type": "shadow", "$value": "{elevation.pill}" },
      "pill-shadow-on-dark": { "$type": "shadow", "$value": "{elevation.pill-on-dark}" },
      "pill-fg": {
        "$type": "color",
        "$value": "{text.body}",
        "$description": "The selected nav label, which sits on the bright pill in both themes (pill-bg is white 72%, pill-bg-on-dark white 92%), so it stays dark and takes no -on-dark counterpart. The unselected labels sit on the glass and read --port-glass-fg-muted instead."
      }
    },

    "topbar": {
      "height": { "$type": "dimension", "$value": { "value": 38, "unit": "px" }, "$description": "Utility strip painted height, 38px. The a11y and language buttons carry their own hit area (p-1.5 around the glyph/label); this is the strip, not the tap target." },
      "bg": { "$type": "color", "$value": "{background.utility}" },
      "bg-on-dark": { "$type": "color", "$value": "{background.ink}", "$description": "The strip on an ink ground: ink itself, so it blends into the page and the utility-bar hairline is what separates it. One shared component across the light portfolio and the ink marketing site." },
      "fg": {
        "$type": "color",
        "$value": "{text.secondary}",
        "$description": "EN label and a11y glyph. The tightest contrast pair in the system; see invariant C1."
      },
      "fg-on-dark": { "$type": "color", "$value": "{surface.glass-fg-muted-on-dark}", "$description": "EN label and a11y glyph on the ink strip." },
      "fg-hover": { "$type": "color", "$value": "{text.body}" },
      "fg-hover-on-dark": { "$type": "color", "$value": "{surface.glass-fg-on-dark}" },
      "lang-min-width": {
        "$type": "dimension",
        "$value": { "value": 20, "unit": "px" },
        "$description": "Fixed floor so the justify-end row does not shift as the label width changes."
      }
    },

    "header": {
      "max-width": {
        "$type": "dimension",
        "$value": { "value": 1728, "unit": "px" },
        "$description": "The header shell cap on wide screens (utility bar + nav band). Wider than the max-w-5xl (1024px) content column on purpose, so the logo→icons composition breathes on ultrawide instead of stranding a narrow strip. site-header.tsx and portfolio-top-bar.tsx read var(--component-header-max-width)."
      },
      "edge-inset": {
        "$type": "dimension",
        "$value": { "value": 111, "unit": "px" },
        "$description": "Desktop (md+) inset pulling the header logo and both icon clusters in from the header band's edges, and the top bar's right cluster by the same amount so the icon columns line up. One home: site-header.tsx (md:px), portfolio-top-bar.tsx (md:pr on the strip's inner row), and the cookie banner's right edge all read var(--component-header-edge-inset). Mobile keeps the gutter."
      },
      "band-pad-top": {
        "$type": "dimension",
        "$value": { "value": 18, "unit": "px" },
        "$description": "Fixed top padding of the header band, the gap between the top bar and the nav row. One home: portfolio.css .port-head-band padding-top reads var(--component-header-band-pad-top). The band's bottom padding still collapses on scroll; only the top is pinned."
      }
    },

    "header-logo": {
      "height": {
        "$type": "dimension",
        "$value": { "value": 2.8505532, "unit": "rem" },
        "$description": "Comb-mark height in the site header (2.79466rem reduced 15% to 2.375461rem, then increased 20%). Single home: site-header.tsx reads var(--component-header-logo-height); no source literal duplicates it."
      },
      "stretch-x": {
        "$type": "number",
        "$value": 1.03,
        "$description": "scaleX. The SVG keeps preserveAspectRatio, so width alone would only centre it."
      }
    },

    "eyebrow": {
      "$description": "The kicker above a page title: a short label in brand blue at 14px, the same treatment the /ame docs breadcrumb carries. One home for the case-studies page eyebrow; components/mdx/eyebrow.tsx reads both vars. Thickness is not a token here: the eyebrow uses the shared docs bold (the `font-medium` reset to 400 + synthesized header stroke under .ame-docs-root), the same the breadcrumb and headings use, so it matches rather than re-declaring a weight.",
      "color": { "$type": "color", "$value": "{text.brand}", "$description": "Brand blue, the links-and-accents colour. Matches the docs breadcrumb (--color-fd-primary -> --port-brand -> text.brand)." },
      "size": { "$type": "dimension", "$value": "{font.size.14}", "$description": "14px: small enough to read as a kicker above the title, not a second heading." }
    },

    "term-sheet-card": {
      "$description": "The case-study term sheet card's own grid spacing, distinct from the shared expertise card. The term sheet packs longer facts (roles, dates, a stack) than the home's short skill lists, so its columns size to content and wrap to new rows (auto-fit) instead of cramming into a fixed five. Bound by components/portfolio/expertise-card.tsx in its term-sheet variant.",
      "col-min": { "$type": "dimension", "$value": { "value": 6, "unit": "rem" }, "$description": "Smallest a fact column shrinks to before auto-fit wraps it to a new row (96px). Narrow enough that all the facts pack into one row inside the 87%-width grid, wide enough that a role or a date still reads on one or two lines, not a stack of broken words." },
      "item-gap": { "$type": "dimension", "$value": { "value": 1.5, "unit": "rem" }, "$description": "Space between fact columns and between wrapped rows (24px)." }
    },

    "accessibility": {
      "width": {
        "$type": "dimension",
        "$value": { "value": 18, "unit": "rem" },
        "$description": "Panel width (288px) for the accessibility, contact, and cookie panels. Single home: portfolio.css --port-panel-w reads it (a D1 exception). At md, --port-panel-w is recomputed against the work-grid column; that calc is CSS-only and named in decisions.md."
      },
      "panel-radius": { "$type": "dimension", "$value": "{radius.2xl}" },
      "trigger-radius": { "$type": "dimension", "$value": "{radius.pill}" },
      "raise": {
        "$type": "number",
        "$value": 10,
        "$description": "Pill anchor lift above the trigger, in vh. DTOS dimension admits px and rem only; see contract.md > Spec gaps.",
        "$extensions": { "org.metis.css": { "unit": "vh" } }
      }
    },

    "splash": {
      "$description": "How the splash leaves: a quick fade, on the enter curve. The mask-cutout reveal that lived here was removed; see decisions D-38.",
      "cycle": {
        "$type": "duration",
        "$value": "{duration.cycle}",
        "$description": "One logo cycle. Read by no rule; its client is invariant P4, which holds logo-bounce.tsx LOGO_CYCLE_MS to it."
      },
      "fade": {
        "$type": "duration",
        "$value": "{duration.long}",
        "$description": "The dismiss, 800ms. loading-screen.tsx FADE_MS must equal it (invariant P5), and it is also the floor the splash adds to its own 6540ms before the page is interactive."
      },
      "fade-ease": {
        "$type": "cubicBezier",
        "$value": "{motion.enter-ease}",
        "$description": "Fast off the mark, settling at the end, so the page reads as arriving rather than the overlay as leaving."
      }
    },

    "card": {
      "bg": { "$type": "color", "$value": "{surface.card-glass}" },
      "min-height": { "$type": "dimension", "$value": { "value": 320, "unit": "px" } },
      "radius": { "$type": "dimension", "$value": "{radius.xl}" },
      "shadow": { "$type": "shadow", "$value": "{elevation.card}" },
      "shadow-hover": { "$type": "shadow", "$value": "{elevation.card-hover}" },
      "lift": {
        "$type": "dimension",
        "$value": { "value": -0.125, "unit": "rem" },
        "$description": "hover translate-y."
      }
    },

    "pill": {
      "radius": { "$type": "dimension", "$value": "{radius.pill}" },
      "bg-rest": { "$type": "color", "$value": "{color.ink-alpha.08}" },
      "bg-hover": { "$type": "color", "$value": "{color.ink-alpha.14}" }
    },

    "contact-row": {
      "radius": { "$type": "dimension", "$value": "{radius.row}" }
    },
    "icon": {
      "size": {
        "$type": "dimension",
        "$value": { "value": 1, "unit": "rem" },
        "$description": "Shared icon box size (h/w), 16px. One home for the small icons across the header, contact menu, accessibility modal, endorsements, and cookie panel; they read var(--component-icon-size)."
      }
    }
  }
}
